# Review #39 — Scroll-progress needle under the navbar

- **Commit:** `adca0c5` (branch `task/39-scroll-progress-needle`) — merged `--no-ff`
- **Files:** `frontend/src/lib/components/ScrollProgressNeedle.svelte` (new), `frontend/src/routes/+layout.svelte` (+3)
- **Disposition:** pass
- **Gaps:** none

**Evidence (reviewer):** passive scroll listener, rAF-throttled, `scaleX` with origin-left, idle opacity 0.4 → 0.9 while scrolling, 350ms idle dim; `prefers-reduced-motion` early return + `change` listener re-sync; z-40 below navbar z-50; `aria-hidden`; verify loop: check 0/0, lint clean, build + prerender OK.
