# Review #41 — Ledger page unroll for the current role card

- **Commit:** `c615a39` (branch `task/41-ledger-unroll`) — merged `--no-ff`
- **Files:** `frontend/src/lib/components/ExperienceTimeline.svelte` only
- **Disposition:** pass
- **Gaps:** none

**Evidence (reviewer):** unroll only on the `entry.highlight` card (0.7s clip-path, `cubic-bezier(0.22,1,0.36,1)`); badge stamp 0.35s hero-restamp family + gold bloom via `::after`; once-only IO with disconnect; reduced-motion path never attaches observer; `animationend` filtering prevents child-event misfire; scoped styles; data.js/app.css untouched; verify loop: check 0/0, lint clean, build OK.
