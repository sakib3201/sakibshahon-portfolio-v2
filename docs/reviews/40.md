# Review #40 — Impact stats: gold-leaf count-up numerals

- **Commits:** `422755f` (feat) + `70ed047` (fix) on `task/40-stat-countup` — merged `--no-ff`
- **Files:** `frontend/src/lib/components/ImpactStats.svelte` only
- **Disposition:** pass (after one `fix` round)
- **Gaps (fixed):** animated numeral was `aria-hidden` with `aria-label` on a generic span — not reliably exposed to screen readers. Fixed with a static `sr-only` span carrying `stat.value`; animated copy stays `aria-hidden`.

**Evidence (reviewer):** display strings parsed (`20k+`, `~35%`, `~2×`, `4+`) with data.js untouched; once-only IntersectionObserver; 1.2s ease-out quint count-up; divider draws after settle; reduced-motion no-op; verify loop: check 0/0, lint clean, build OK.
