# Review #43 — Project images: ink-wash hover with cursor gold bloom

- **Commit:** `3dc0c46` (branch `task/43-ink-wash`) — merged `--no-ff`
- **Files:** `ProjectCard.svelte`, `ProjectLibraryCard.svelte`, `frontend/src/lib/motion.js` (+146 `initInkWash`)
- **Disposition:** pass
- **Gaps:** none

**Evidence (reviewer):** idle `saturate(0.78) brightness(0.9)` on the well container (fallback medallion included), hover/focus-within `filter: none` over 0.5s; gold radial bloom via `--x`/`--y`, rAF-throttled, `pointer: fine` only, IntersectionObserver on-screen gating; reduced motion stops JS tracking and forces bloom opacity 0; lazy loading/onerror/aria preserved; no new deps; verify loop: check 0/0, lint clean, build OK.

**Post-merge fix (runtime verification):** the scoped hover rules were written as `.group:hover .ink-wash-well` — in a Svelte scoped `<style>` the `.group` part compiles to `.group.svelte-<hash>`, which never matches the plain utility `group` class on the article, so the hover colorize + bloom never fired. Fixed with `:global(.group)` (the pattern `YouTubeSection` already used). Verified in a headless browser on `/` and `/projects`: hover now colorizes and the bloom tracks the cursor; reduced-motion context stays fully static.
